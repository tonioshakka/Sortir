<form method="post" action="{{ path('app_participant_inactif', {'id': participant.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('inactif' ~ participant.id) }}">
    <button class="btn">Supprimée</button>
</form>
