{% extends 'base.html.twig' %}

{% block title %}Edit Participant{% endblock %}

{% block body %}
    <h1 class="former-titre">Gestion de profil</h1>

    {{ include('participant/_form.html.twig', {'button_label': 'Mise à jour'}) }}

    {% if is_granted('ROLE_ADMIN') %}
        <a class="create_class" href="{{ path('app_participant_index') }}">Retour a la liste des participants</a>
    {% endif %}

    {% if is_granted('ROLE_ADMIN') %}
        <a class="create_class" href="{{ path('app_participant_edit', {'id': participant.id, 'token': csrf_token('editer'~participant.id)}) }}">Supprimer le participant</a>
    {% endif %}
{% endblock %}
